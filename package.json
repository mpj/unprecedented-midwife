{
  "name": "unprecedented-midwife",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.2"
  },
  "dependencies": {
    "enzyme": "^2.7.1",
    "react": "^15.4.2",
    "react-addons-test-utils": "^15.4.2",
    "react-di": "^0.3.1",
    "react-dom": "^15.4.2",
    "react-localization": "^0.0.12",
    "react-redux": "^5.0.3",
    "redux": "^3.6.0",
    "standard": "^9.0.2"
  },
  "scripts": {
    "start": "standard && react-scripts start",
    "build": "standard && react-scripts build",
    "test": "standard && react-scripts test --env=jsdom",
    "eject": "standard && react-scripts eject"
  }
}
